{% extends 'base.html.twig' %}
{% block encoding %}<meta charset="UTF-8">{% endblock %}
{% block title %}Connexion - {% endblock %}

{% block body %}

    <H1>Connexion !</H1>
	<br/>
	
    <form  action="{{ path('login') }}" method="post">
		{% if error %}
        	<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
   		{% endif %}
   		 
        <div class="form-group">
            <label for="inputEmail" class="sr-only">Email :</label>
            <input type="email" value="{{ last_username }}" name="_email" id="inputEmail" class="form-control" placeholder="Entrer votre adresse mail" required autofocus>
        </div>
        
        <div class="form-group">
            <label for="inputPassword" class="sr-only">Mot de passe :</label>
            <input type="password" name="_password" id="inputPassword" class="form-control" placeholder="Entrer votre mot de passe" required>
    	</div>
    	
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"> 

    	<button type="submit" class="btn btn-success">Se connecter</button>

        <button type="button" class="btn btn-secondary" onclick="window.location.href = '{{ path('forgotten_password') }}';">Mot de passe oublié</button>
      
    </form>
    
{% endblock %}