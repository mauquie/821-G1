{% extends 'base.html.twig' %}
{% block encoding %}<meta charset="UTF-8">{% endblock %}
{% block title %}Nouveau mot de passe - {% endblock %}
{% block body %}

    <h1>Réinitialisation du mot de passe</h1>
    <p>Veuillez entrer votre <b>nouveau mot de passe</b> ci-dessous :</p>
    {{ form_start(resetPassForm) }}
    
        {{ form_row(resetPassForm.password.first, {
            	'label': 'Mot de passe :', 
            	'attr': {'placeholder': 'Mot de passe...'}
            })
        }}
        
	    {{ form_row(resetPassForm.password.second, {
            	'label': 'Confirmation du mot de passe :', 
            	'attr': {'placeholder': 'Confirmer mot de passe...'}
            })
        }}
         
		<input type="hidden" name="token" value="{{ token }}">  
     	<button type="submit" class="btn btn-success">Valider</button>
    {{ form_end(resetPassForm) }}
        
{% endblock %}
